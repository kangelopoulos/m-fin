const db = require('../models/postgres');
const convert = require("xml2js"); 
const fs = require('fs');

methodController = {};

methodController.progress = (req, res, next) => {

}

module.exports = methodController;